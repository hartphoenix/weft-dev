[U1 16:03] # Review Command

<command_purpose> Perform exhaustive code reviews using multi-agent analysis. </command_purpose>

## Introduction

<role>Senior Code Review Architect with expertise in security, performance, architecture, and quality assurance</role>

## Prerequisites

<requirements>
- Git repository with GitHub CLI (`gh`) installed and authenticated
- Clean main/master branch
- Proper permissions to access the repository
</requirements>

## Main Tasks

### 1. Determine Review Target & Setup (ALWAYS FIRST)

<review_target> #ONLY the contents of scoring-sandbox </review_target>

#### Immediate Actions:

- [ ] Determine review type: PR number (numeric), GitHub URL, file path (.md), or empty (current branch)
- [ ] Check current git branch
- [ ] If ALREADY on the target branch → proceed with analysis on current branch
- [ ] If DIFFERENT branch → checkout the target branch
- [ ] Fetch PR metadata using `gh pr view --json` for title, body, files, linked issues
- [ ] Make sure we are on the branch we are reviewing

#### Protected Artifacts

<protected_artifacts>
The following paths are pipeline artifacts and must never be flagged for deletion:

- `docs/plans/*.md` — Plan files created by `/workflows:plan`
- `docs/solutions/*.md` — Solution documents created during the pipeline
</protected_artifacts>

#### Load Review Agents

Read `compound-engineering.local.md` in the project root. Use `review_agents` from YAML frontmatter. If the markdown body contains review context, pass it to each agent as additional instructions.

#### Parallel Agents to review the PR:

<parallel_tasks>

Run all configured review agents in parallel using Task tool. For each agent in the `review_agents` list:

```
Task {agent-name}(PR content + review context from settings body)
```

Additionally, always run these regardless of settings:
- Task learnings-researcher(PR content) - Search docs/solutions/ for past issues related to this PR's modules and patterns
- Task feature-ui-completeness(PR content + changed files) - Phase 5 audit (see instructions below)
- Task decision-balance-audit(PR content + changed files) - Phase 6 audit (see instructions below)

</parallel_tasks>

### 4. Deep Dive Phases

#### Phase 3: Stakeholder Perspective Analysis

1. **Developer Perspective** — How easy is this to understand and modify?
2. **Operations Perspective** — How do I deploy this safely?
3. **End User Perspective** — Is the feature intuitive?
4. **Security Team Perspective** — What's the attack surface?

#### Phase 4: Scenario Exploration

- [ ] **Happy Path**: Normal operation with valid inputs
- [ ] **Invalid Inputs**: Null, empty, malformed data
- [ ] **Boundary Conditions**: Min/max values, empty collections
- [ ] **Concurrent Access**: Race conditions, deadlocks
- [ ] **Network Issues**: Timeouts, partial failures
- [ ] **Security Attacks**: Injection, overflow, DoS

#### Phase 5: Feature-UI Completeness Audit (subagent)

Run as a Task subagent with the diff/file list. The agent's instructions:

> For every state field, config value, or mechanic introduced in the PR, audit:
>
> 1. **Visibility**: Is every piece of internal state that affects gameplay/behavior surfaced to the user somewhere in the UI? Flag any "invisible mechanics" — systems that work in code but give the player/user zero feedback.
> 2. **Feedback loop**: When the user takes an action (purchase, toggle, input), is there immediate visible confirmation that something changed?
> 3. **Consumables & counters**: Are quantities shown before purchase, after purchase, and on consumption? Can the user tell when they're running low?
> 4. **Dead-end UX**: Can the user reach a state where a feature exists but they have no way to discover or understand it from the UI alone?
>
> Read the source files. For each finding, report: field name, where it's set, where it's used in logic, whether it's rendered anywhere, and your verdict. Return a structured list of findings with suggested severity (P1/P2/P3).

#### Phase 6: Decision & Balance Audit (subagent)

Run as a Task subagent with the diff/file list. The agent's instructions:

> Analyze every player-facing decision point introduced in the PR (purchases, toggles, accept/deny choices, sliders, resource allocation). For each one, answer:
>
> 1. **Dominant strategy**: Is there an option that is always correct regardless of game state? If "yes to everything" or "no to everything" is optimal, flag it.
> 2. **Meaningful trade-off**: Does choosing one option make another option more valuable, or does every choice exist in isolation?
> 3. **Resource pressure**: Do resource constraints (money, time, sanity, etc.) actually force the player to choose, or can they afford everything?
> 4. **Downside existence**: Does saying "no" (or choosing the conservative option) ever produce a better outcome than "yes"? Under what conditions?
> 5. **Escalation curve**: As the game progresses, do decisions get harder (tighter resources, higher stakes), or does the player's advantage compound without limit?
>
> Read the game logic, economy constants, and event/effect systems. Trace actual numeric outcomes for accept-vs-reject and buy-vs-skip decisions. Return a structured list of balance findings with suggested severity:
> - P1 if a dominant strategy makes an entire mechanic decorative
> - P2 if trade-offs exist but are too weak to matter in practice
> - P3 if balance is close but could be tightened

### 4. Simplification and Minimalism Review

Run the Task code-simplicity-reviewer() to see if we can simplify the code.

### 5. Findings Synthesis and Todo Creation Using file-todos Skill

<critical_requirement> ALL findings MUST be stored in the todos/ directory using the file-todos skill. Create todo files immediately after synthesis. </critical_requirement>

#### Step 1: Synthesize All Findings

- [ ] Collect findings from all parallel agents
- [ ] Surface learnings-researcher results: if past solutions are relevant, flag them as "Known Pattern" with links to docs/solutions/ files
- [ ] Discard any findings that recommend deleting files in `docs/plans/` or `docs/solutions/`
- [ ] Categorize by type: security, performance, architecture, quality, etc.
- [ ] Assign severity levels: P1 (CRITICAL), P2 (IMPORTANT), P3 (NICE-TO-HAVE)
- [ ] Remove duplicate or overlapping findings
- [ ] Estimate effort for each finding (Small/Medium/Large)

#### Step 2: Create Todo Files Using file-todos Skill

Use the file-todos skill to create todo files for ALL findings immediately.

- Create todo files directly using Write tool
- Use standard template from `.claude/skills/file-todos/assets/todo-template.md`
- Follow naming convention: `{issue_id}-pending-{priority}-{description}.md`

**Examples:**
```
001-pending-p1-path-traversal-vulnerability.md
002-pending-p1-api-response-validation.md
003-pending-p2-concurrency-limit.md
004-pending-p3-unused-parameter.md
```

#### Step 3: Summary Report

After creating all todo files, present comprehensive summary:

```markdown
## Code Review Complete

**Review Target:** PR #XXXX - [PR Title]
**Branch:** [branch-name]

### Findings Summary:
- **Total Findings:** [X]
- **CRITICAL (P1):** [count] - BLOCKS MERGE
- **IMPORTANT (P2):** [count] - Should Fix
- **NICE-TO-HAVE (P3):** [count] - Enhancements

### Created Todo Files:
[list all created todo files]

### Next Steps:
1. Address P1 Findings (CRITICAL - must be fixed before merge)
2. Triage All Todos: `ls todos/*-pending-*.md`
```

### Important: P1 Findings Block Merge

Any P1 (CRITICAL) findings must be addressed before merging the PR. Present these prominently and ensure they're resolved before accepting the PR.
  -> Bash: git -C /Users/rhhart/Documents/GitHub/schelling-points branch --show-current
  -> Glob: compound-engineering.local.md
  -> Glob: scoring-sandbox/**/*
  -> Glob: .claude/skills/file-todos/assets/todo-template.md
[A6 16:04] Let me get the actual source files and key configuration.
  -> Glob: scoring-sandbox/**/* in schelling-points
  -> Read: todo-template.md
  -> Glob: docs/solutions/**/*.md
[A10 16:04] Let me get the actual source files, excluding node_modules.
  -> Bash: find /Users/rhhart/Documents/GitHub/schelling-points/scoring-sandbox -not -path 
  -> TodoWrite
  -> Read: package.json
  -> Read: App.tsx
  -> Read: main.tsx
  -> Read: types.ts
  -> Read: embeddings.ts
  -> Read: projection.ts
  -> Read: scoring.ts
  -> Read: InputPanel.tsx
  -> Read: ModeSelector.tsx
  -> Read: ScatterPlot.tsx
  -> Read: ScoreTable.tsx
  -> Read: vite.config.ts
  -> Read: tsconfig.json
  -> Read: index.html
  -> Read: README.md
[A28 16:04] All source files read. Now launching parallel review agents.
  -> TodoWrite
  -> Task
  -> Task
  -> Task
  -> Task
  -> Task
[A35 16:07] All agents complete. Now synthesizing findings and creating todo files.
  -> TodoWrite
  -> Bash: ls /Users/rhhart/Documents/GitHub/schelling-points/todos/ 2>/dev/null || echo "d
  -> Bash: mkdir -p /Users/rhhart/Documents/GitHub/schelling-points/todos
[A39 16:08] Now creating all todo files. I've deduplicated findings across all 5 review agents into 15 consolidated items.
  -> Write: 001-pending-p1-race-condition-concurrent-computes.md
  -> Write: 002-pending-p1-scoring-modes-invisible-to-user.md
  -> Write: 003-pending-p2-react-key-index-dynamic-list.md
  -> Write: 004-pending-p2-scatter-plot-feedback-gaps.md
  -> Write: 005-pending-p2-sort-indicator-missing.md
  -> Write: 006-pending-p2-backend-dependency-invisible.md
  -> Write: 007-pending-p2-unsanitized-error-content.md
  -> Write: 008-pending-p2-vector-dimension-validation.md
  -> Write: 009-pending-p2-brittle-error-handling-embeddings.md
  -> Write: 010-pending-p2-duplicate-logic-cleanup.md
  -> Write: 011-pending-p3-embedding-response-validation.md
  -> Write: 012-pending-p3-pca-main-thread-bundle-size.md
  -> Write: 013-pending-p3-type-safety-improvements.md
  -> Write: 014-pending-p3-magic-numbers-response-limits.md
  -> Write: 015-pending-p3-ux-polish-results-loading.md
  -> TodoWrite
[A56 16:10] ## Code Review Complete

**Review Target:** `scoring-sandbox/` directory on branch `scoring-dashboard`
**Agents:** Security, Architecture, Performance, UI Completeness, Code Simplicity

### What's Working Well

Before the findings -- this is a clean codebase. The review agents unanimously noted:
- **Solid separation of concerns** -- pure math in `lib/`, API in `embeddings.ts`, UI in `components/`
- **Good error handling** for a sandbox -- checks HTTP status, body errors, zero-magnitude vectors
- **Minimal dependency footprint** -- only recharts and ml-pca beyond React
- **TypeScript strict mode** with `noUnusedLocals` and `noUnusedParameters`
- **No security vulnerabilities** -- no XSS, no secrets, no `dangerouslySetInnerHTML`

### Findings Summary

| Priority | Count | Status |
|----------|-------|--------|
| **P1 (CRITICAL)** | 2 | BLOCKS next iteration |
| **P2 (IMPORTANT)** | 8 | Should fix |
| **P3 (NICE-TO-HAVE)** | 5 | Polish / future |
| **Total** | **15** | |

### P1 -- Must Fix

| # | Finding | Effort |
|---|---------|--------|
| [001](todos/001-pending-p1-race-condition-concurrent-computes.md) | Race condition on concurrent computes -- no AbortController, stale results can overwrite current | Small |
| [002](todos/002-pending-p1-scoring-modes-invisible-to-user.md) | Scoring mode definitions completely invisible to user -- core mechanic is opaque | Small |

### P2 -- Should Fix

| # | Finding | Effort |
|---|---------|--------|
| [003](todos/003-pending-p2-react-key-index-dynamic-list.md) | `key={i}` on dynamic add/remove list causes React reconciliation bugs | Small |
| [004](todos/004-pending-p2-scatter-plot-feedback-gaps.md) | Centroid not plotted + scatter plot ignores active scoring mode | Medium |
| [005](todos/005-pending-p2-sort-indicator-missing.md) | No sort indicator when table reshuffles on mode toggle | Small |
| [006](todos/006-pending-p2-backend-dependency-invisible.md) | Backend dependency (ollama) invisible until first failure | Small |
|  [...truncated]
  -> TodoWrite
[U2 16:12] move todos folder into scoring-sandbox and update any file path references that this move changes, if any
  -> Bash: mv /Users/rhhart/Documents/GitHub/schelling-points/todos /Users/rhhart/Documents
[A59 16:12] Now let me check if any of the todo files contain internal file path references that need updating. The todos reference source files like `src/App.tsx` -- those are already relative and still correct from inside `scoring-sandbox/`. Let me verify there are no absolute or repo-root-relative paths that broke.
  -> Grep: todos/ in todos
[A61 16:12] No self-references to the `todos/` path inside the todo files. The `Affected files` sections all use relative paths like `src/App.tsx` which are correct from within `scoring-sandbox/`. Move is clean.
[U3 16:13] conduct a structured self-review of the artifacts created by the review workflow and apply the /handoff-test
  -> Skill: 
[U4 16:13] Base directory for this skill: /Users/rhhart/.claude/skills/handoff-test

# Handoff Test

## The Naive Reader

A fresh Claude instance with no session history. Technically competent — can read the codebase, run code, check git history. Has access to standard tooling, CLAUDE.md, and memory files. But has seen nothing from this conversation: no decisions made, options rejected, constraints discovered, or terminology coined. The only things that exist for the naive reader are what's written in persistent artifacts.

The test: could that instance pick up the artifact and execute without asking clarifying questions?

## Process

1. Identify what was created or modified this session. Read each artifact in full.
2. From the naive reader perspective, find implicit dependencies:
   - Session-local terminology used without definition
   - References to "the bug," "the approach," "what we discussed" without grounding
   - Decisions recorded without rationale (a future reader needs *why* to know when the decision expires)
   - Scope assumed but not stated
   - Next steps that depend on conversation memory
3. Report gaps. For each: what's missing, where it goes, concrete suggested text. Offer to apply fixes.

If the artifact passes, say so in one sentence.
[A65 16:13] Let me read all 15 todo files to audit them from a naive reader's perspective.
  -> Read: 001-pending-p1-race-condition-concurrent-computes.md
  -> Read: 002-pending-p1-scoring-modes-invisible-to-user.md
  -> Read: 003-pending-p2-react-key-index-dynamic-list.md
  -> Read: 004-pending-p2-scatter-plot-feedback-gaps.md
  -> Read: 005-pending-p2-sort-indicator-missing.md
  -> Read: 006-pending-p2-backend-dependency-invisible.md
  -> Read: 007-pending-p2-unsanitized-error-content.md
  -> Read: 008-pending-p2-vector-dimension-validation.md
  -> Read: 009-pending-p2-brittle-error-handling-embeddings.md
  -> Read: 010-pending-p2-duplicate-logic-cleanup.md
  -> Read: 011-pending-p3-embedding-response-validation.md
  -> Read: 012-pending-p3-pca-main-thread-bundle-size.md
  -> Read: 013-pending-p3-type-safety-improvements.md
  -> Read: 014-pending-p3-magic-numbers-response-limits.md
  -> Read: 015-pending-p3-ux-polish-results-loading.md
[A81 16:14] ## Handoff Test Results

I read all 15 todo files from the naive-reader perspective. Here's what a fresh Claude instance -- with access to the codebase but zero session context -- would stumble on.

---

### Gap 1: No index/overview for the todos directory

**What's missing:** A naive reader lands in `scoring-sandbox/todos/` and finds 15 numbered markdown files with no context for what they are, what the priority scale means, or how they relate to each other.

**Where it goes:** `scoring-sandbox/todos/README.md`

**Suggested text:**
```markdown
# Review Todos

Output of a code review conducted 2026-02-18 on the `scoring-dashboard` branch,
scoped to `scoring-sandbox/` only.

## Priority Scale

- **P1 (CRITICAL)** — Must fix before merging. Blocks next iteration.
- **P2 (IMPORTANT)** — Should fix. Correctness, UX, or code quality issues.
- **P3 (NICE-TO-HAVE)** — Polish, optimization, or future-proofing.

## File Path Convention

All `src/` paths in these todos are relative to `scoring-sandbox/`.

## Known Overlaps

Several todos touch the same files. If working them in sequence, be aware:

- **007 + 009** — Both modify error handling in `src/lib/embeddings.ts`. Do together.
- **001 + 010** — Both modify handlers in `src/App.tsx`. 010's cleanup simplifies 001.
- **003 + 010 + 014** — All touch the responses state model or InputPanel. 003 changes
  the state shape from `string[]` to `{id, text}[]`, which cascades to the filter logic
  in 010 and the constants in 014.
```

---

### Gap 2: Missing acceptance criteria on three todos

**What's missing:** Todos 011, 012, and 015 skip the `## Acceptance Criteria` section, breaking the template pattern. A naive reader following the template expects checkboxes to verify completion.

**Where it goes:** Each file, after `## Affected files`

**011** needs:
```markdown
## Acceptance Criteria

- [ ] `fetchEmbedding` throws on non-array or non-numeric embedding values
- [ ] NaN values cannot propagate to scoring functions
```

**012 [...truncated]
[U5 16:17] reconsider priorities in light of the fact that this scoring dashboard is merely a design tool for a 4-person team to make informed design decisions for a game -- critical updates should come first in the priority order if they serve this purpose best. then do fixes that still apply
[A83 16:18] Good call. Let me reframe through the lens of "does this help or block a 4-person team making design decisions with this tool?"

**Priority shifts:**

| Todo | Current | New | Rationale |
|------|---------|-----|-----------|
| 001 (race condition) | P1 | P3 | Team clicks Compute manually; race is near-impossible in practice |
| 002 (modes invisible) | P1 | P1 | Stays -- this IS the tool's purpose |
| 003 (key={i}) | P2 | P3 | 4-8 inputs, rarely removed; minor annoyance at most |
| 004 (scatter feedback) | P2 | **P1** | Team can't visually verify why scores differ without centroid |
| 005 (sort indicator) | P2 | P2 | Stays -- helps team compare rankings across modes |
| 006 (backend invisible) | P2 | P2 | Stays -- onboarding friction for team members |
| 007 (unsanitized errors) | P2 | P3 | Local tool, trusted users, React escapes by default |
| 008 (vector dimension) | P2 | P3 | Defensive coding; ollama API is consistent |
| 009 (brittle errors) | P2 | P2 | Stays -- wrong error messages confuse debugging + hardcoded model blocks experimentation |
| 010 (duplicate logic) | P2 | P3 | Code quality; doesn't affect design utility |

Now applying priority changes + handoff fixes together.
  -> Bash: cd /Users/rhhart/Documents/GitHub/schelling-points/scoring-sandbox/todos && mv 0
[A85 16:19] Now updating frontmatter, adding rationale, handoff fixes (acceptance criteria, dependencies, cascade notes), and the README -- all in parallel.
  -> Edit: 001-pending-p3-race-condition-concurrent-computes.md
  -> Edit: 001-pending-p3-race-condition-concurrent-computes.md
  -> Edit: 003-pending-p3-react-key-index-dynamic-list.md
  -> Edit: 003-pending-p3-react-key-index-dynamic-list.md
  -> Edit: 003-pending-p3-react-key-index-dynamic-list.md
  -> Edit: 004-pending-p1-scatter-plot-feedback-gaps.md
  -> Edit: 004-pending-p1-scatter-plot-feedback-gaps.md
  -> Edit: 007-pending-p3-unsanitized-error-content.md
  -> Edit: 007-pending-p3-unsanitized-error-content.md
  -> Edit: 007-pending-p3-unsanitized-error-content.md
  -> Edit: 008-pending-p3-vector-dimension-validation.md
  -> Edit: 008-pending-p3-vector-dimension-validation.md
  -> Edit: 010-pending-p3-duplicate-logic-cleanup.md
  -> Edit: 010-pending-p3-duplicate-logic-cleanup.md
  -> Edit: 010-pending-p3-duplicate-logic-cleanup.md
  -> Edit: 009-pending-p2-brittle-error-handling-embeddings.md
  -> Edit: 011-pending-p3-embedding-response-validation.md
  -> Edit: 012-pending-p3-pca-main-thread-bundle-size.md
  -> Edit: 015-pending-p3-ux-polish-results-loading.md
  x Edit failed: <tool_use_error>File has not been read yet. Read it first before writing to it.</tool_use_error>
  -> Write: README.md
  x Write failed: <tool_use_error>Sibling tool call errored</tool_use_error>
[A106 16:19] Two edits failed due to a sibling error. Let me re-read and retry.
  -> Read: 015-pending-p3-ux-polish-results-loading.md
  -> Edit: 015-pending-p3-ux-polish-results-loading.md
  -> Write: README.md
[A110 16:20] All done. Here's what changed:

**Priority reframes (6 files renamed + frontmatter + rationale in work logs):**
- 001: P1 → P3 (race condition -- team won't hit this manually)
- 003: P2 → P3 (key={i} -- minor with small input counts)
- 004: P2 → **P1** (scatter plot is the team's primary visualization tool)
- 007: P2 → P3 (unsanitized errors -- local tool, trusted users)
- 008: P2 → P3 (vector validation -- ollama API is consistent)
- 010: P2 → P3 (duplicate logic -- code quality, not design utility)

**Handoff fixes applied:**
- Created [todos/README.md](scoring-sandbox/todos/README.md) -- context, priority scale, file path convention, overlap map
- Added missing acceptance criteria to 011, 012, 015
- Added `dependencies` cross-references to 007↔009 and 010→003
- Expanded 003's affected files to show full cascade of the state shape change
- Each reframed todo has a rationale entry in its work log explaining *why* the priority changed

**Final shape:**

| Priority | Count | Todos |
|----------|-------|-------|
| P1 | 2 | 002 (modes invisible), 004 (scatter plot gaps) |
| P2 | 3 | 005 (sort indicator), 006 (backend invisible), 009 (brittle errors) |
| P3 | 10 | Everything else |
[U6 16:21] Base directory for this skill: /Users/rhhart/.claude/skills/handoff-test

# Handoff Test

## The Naive Reader

A fresh Claude instance with no session history. Technically competent — can read the codebase, run code, check git history. Has access to standard tooling, CLAUDE.md, and memory files. But has seen nothing from this conversation: no decisions made, options rejected, constraints discovered, or terminology coined. The only things that exist for the naive reader are what's written in persistent artifacts.

The test: could that instance pick up the artifact and execute without asking clarifying questions?

## Process

1. Identify what was created or modified this session. Read each artifact in full.
2. From the naive reader perspective, find implicit dependencies:
   - Session-local terminology used without definition
   - References to "the bug," "the approach," "what we discussed" without grounding
   - Decisions recorded without rationale (a future reader needs *why* to know when the decision expires)
   - Scope assumed but not stated
   - Next steps that depend on conversation memory
3. Report gaps. For each: what's missing, where it goes, concrete suggested text. Offer to apply fixes.

If the artifact passes, say so in one sentence.
[A112 16:21] Let me re-read the modified artifacts to test the updated state.
  -> Read: README.md
  -> Read: 001-pending-p3-race-condition-concurrent-computes.md
  -> Read: 004-pending-p1-scatter-plot-feedback-gaps.md
  -> Read: 003-pending-p3-react-key-index-dynamic-list.md
  -> Read: 007-pending-p3-unsanitized-error-content.md
  -> Read: 009-pending-p2-brittle-error-handling-embeddings.md
  -> Read: 010-pending-p3-duplicate-logic-cleanup.md
  -> Bash: ls -1 /Users/rhhart/Documents/GitHub/schelling-points/scoring-sandbox/todos/
[A121 16:22] ## Handoff Test -- Round 2

Checked all 16 artifacts (README + 15 todos). Previous gaps were addressed. One minor new issue; otherwise this passes.

### Resolved from Round 1

- **README exists** with context, priority scale, path convention, overlap map
- **Acceptance criteria** added to 011, 012, 015
- **Dependencies** cross-referenced on 007↔009, 010→003
- **003 cascade** fully documented (5 affected locations + note)
- **Priority rationale** in work logs of all 6 reframed todos
- **Filenames match frontmatter** -- verified all 15, all consistent

### One Minor Remaining Quirk

**007 `dependencies: ["009"]` and 009 `dependencies: ["007"]` is a circular dependency.** The `dependencies` field conventionally means "do X first," but here it means "co-modify, do together." A naive reader seeing both frontmatter entries without reading the README might interpret this as a deadlock. The README's "Do together" note resolves the ambiguity, but the frontmatter semantics are slightly off.

**Verdict:** Not worth fixing. The README is the first file in the directory and states the intent clearly. A competent reader will check the README before executing individual todos.

### Pass

These artifacts pass the handoff test. A fresh instance with access to the README and codebase could pick up any todo and execute without clarifying questions.
