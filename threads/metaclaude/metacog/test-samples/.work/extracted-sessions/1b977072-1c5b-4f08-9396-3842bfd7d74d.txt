[U1 05:19] # Work Plan Execution Command

Execute a work plan efficiently while maintaining quality and finishing features.

## Introduction

This command takes a work document (plan, specification, or todo file) and executes it systematically. The focus is on **shipping complete features** by understanding requirements quickly, following existing patterns, and maintaining quality throughout.

## Input Document

<input_document> #on Phase 3 ONLY of docs/plans/2026-02-20-feat-mind-meld-cooperative-redesign-plan.md — stop once Phase 3 is complete and get further instructions.

Context from Phase 1 + Phase 2 (completed on hart/mind-meld branch):

WHAT EXISTS FROM PHASE 1:
- `stemmer` npm package installed (stemmer@2.0.1)
- src/server/stemmer.ts — exports `stem(word: string): string`
- src/server/vocab.ts — exports `Vocab` interface, `loadVocab(): Vocab`, `nearestWord(centroidVec: number[], vocab: Vocab): string`
- scripts/build-vocab-embeddings.ts — one-time build script
- data/google-10000-english-no-swears.txt — vendored 10k word list

WHAT EXISTS FROM PHASE 2:
- src/config.ts — MAX_ROUNDS=20, GUESS_SECS=10, REVEAL_SECS=3, MAX_SCORING_RETRIES=3. Old names (ROUNDS_PER_GAME, SCORE_SECS, DIFFICULTY_SCHEDULE) fully removed.
- src/server/scoring.ts — ScoringResult now includes `centroidWord: string`. `scoreGuesses(guesses, vocab?)` accepts optional Vocab param. When vocab is undefined, centroidWord returns ''. Single-entry case returns the guess itself as centroidWord.
- src/server/meld.ts — exports `filterPromptRepetitions(guesses, prompt): Map` and `detectMeld(validGuesses): boolean`. Both use Porter2 stemmer.
- src/server/play.ts — `pickCategory`/`fillQueue`/`shuffle` replaced with `pickRandomPrompt(categories)`. Config references updated. BUT: still uses `category` naming in variables/Phase types (Phase 3 renames to `prompt`). `scoreRound` still calls `scoreGuesses(guesses)` without vocab (Phase 3 threads state.vocab through). `categoryQueues` still on Game class in types.ts (Phase 3 removes).

CURRENT STATE OF TYPES (Phase 3 rewrites these):
- src/types.ts — still has SCORE_STATE, SCORES_READY (Phase 3 replaces with REVEAL_STATE, REVEAL_READY, adds GAME_END, CONTINUE_PROMPT, CONTINUE_VOTE)
- src/server/types.ts — Phase union still has SCORES (Phase 3 renames to REVEAL, adds CONTINUE). Game class still has categoryQueues (Phase 3 removes, adds centroidHistory, currentPrompt, scoringRetries). RoundScore.category → Phase 3 renames to .prompt. State class needs vocab field added.

KEY CONSTRAINTS:
- DO NOT open a PR. Commit to hart/mind-meld only.
- Work through sub-tasks 3a → 3i IN ORDER. Commit after each sub-task if possible.
- `endGame()` and `buildPlayerHistory()` MUST be extracted as named functions (not inline) — Phase 4 reuses them.
- `newGuessPhase` must keep a stable signature `(round: number, prompt: string)` — Phase 4 calls it.
- For round 20 without meld: just call `endGame()` for now. Phase 4 changes this to enter CONTINUE phase.
- currentGameState returns broadcast messages. GAME_END is unicast — handle in endGame() directly.
- Pass `state` through the call chain (onTickGame → scoreRound → goToNextRound → endGame) so all functions access state.vocab, state.categories, state.lounge, state.games.
- Tests use vitest (import from 'vitest'), not bun:test.
- The play.test.ts failure (bun:test import) is pre-existing — ignore it.
- Run `bunx vitest run` to verify tests pass.
- Project must compile after Phase 3. </input_document>

## Execution Workflow

### Phase 1: Quick Start

1. **Read Plan and Clarify**

   - Read the work document completely
   - Review any references or links provided in the plan
   - If anything is unclear or ambiguous, ask clarifying questions now
   - Get user approval to proceed
   - **Do not skip this** - better to ask questions now than build the wrong thing

2. **Setup Environment**

   First, read the base branch from config and check the current branch:

   ```bash
   # Read base branch from .claude/compound-engineering.md frontmatter
   base_branch=$(grep '^base_branch:' .claude/compound-engineering.md 2>/dev/null | sed 's/base_branch: *//')
   if [ -z "$base_branch" ]; then
     base_branch=$(git symbolic-ref refs/remotes/origin/HEAD 2>/dev/null | sed 's@^refs/remotes/origin/@@')
   fi
   if [ -z "$base_branch" ]; then
     base_branch=$(git rev-parse --verify origin/main >/dev/null 2>&1 && echo "main" || echo "master")
   fi

   current_branch=$(git branch --show-current)
   ```

   **Then, check for an existing feature branch related to this plan.** Branch
   convention is `<person>/<short-description>` (see CLAUDE.md team table for
   person names). Check if a branch matching this plan already exists locally:

   ```bash
   git branch --list "*/*"
   ```

   **If a matching feature branch exists** (from a previous work pass on the same plan):
   - Ask: "Found existing branch `[branch_name]`. Continue on this branch?"
   - If yes, check it out and proceed to step 3
   - If no, follow "Create a new branch" below

   **If already on a feature branch** (not the base branch):
   - Ask: "Continue working on `[current_branch]`, or create a new branch?"
   - If continuing, proceed to step 3
   - If creating new, follow "Create a new branch" below

   **If on the base branch with no matching feature branch:**

   **Create a new branch**
   ```bash
   git pull origin [base_branch]
   git checkout -b <person>/<short-description>
   ```
   Use the `<person>/<short-description>` convention from CLAUDE.md (e.g., `hart/lobby-onboarding`, `ulysse/scoring-algorithm`).

   **Continue on the base branch** (requires explicit user confirmation)
   - Only proceed after user explicitly says "yes, commit to `[base_branch]`"
   - Never commit directly to the base branch without explicit permission

3. **Ensure Working Directories**

   ```bash
   mkdir -p .claude/todos/agent
   ```

4. **Create Todo List**
   - Use TodoWrite to break plan into actionable tasks
   - Include dependencies between tasks
   - Prioritize based on what needs to be done first
   - Include testing and quality check tasks
   - Keep tasks specific and completable

### Phase 2: Execute

1. **Task Execution Loop**

   For each task in priority order:

   ```
   while (tasks remain):
     - Mark task as in_progress in TodoWrite
     - Read any referenced files from the plan
     - Look for similar patterns in codebase
     - Implement following existing conventions
     - Write tests for new functionality
     - Run tests after changes
     - Mark task as completed in TodoWrite
     - Mark off the corresponding checkbox in the plan file ([ ] → [x])
     - Evaluate for incremental commit (see below)
   ```

   **IMPORTANT**: Always update the original plan document by checking off completed items. Use the Edit tool to change `- [ ]` to `- [x]` for each task you finish. This keeps the plan as a living document showing progress and ensures no checkboxes are left unchecked.

2. **Incremental Commits**

   After completing each task, evaluate whether to create an incremental commit:

   | Commit when... | Don't commit when... |
   |----------------|---------------------|
   | Logical unit complete (model, service, component) | Small part of a larger unit |
   | Tests pass + meaningful progress | Tests failing |
   | About to switch contexts (backend → frontend) | Purely scaffolding with no behavior |
   | About to attempt risky/uncertain changes | Would need a "WIP" commit message |

   **Heuristic:** "Can I write a commit message that describes a complete, valuable change? If yes, commit. If the message would be 'WIP' or 'partial X', wait."

   **Commit workflow:**
   ```bash
   # 1. Verify tests pass (use project's test command)
   # 2. Stage only files related to this logical unit (not `git add .`)
   git add <files related to this logical unit>
   # 3. Commit with conventional message
   git commit -m "feat(scope): description of this unit"
   ```

3. **Follow Existing Patterns**

   - The plan should reference similar code - read those files first
   - Match naming conventions exactly
   - Reuse existing components where possible
   - Follow project coding standards (see CLAUDE.md)
   - When in doubt, grep for similar implementations

4. **Test Continuously**

   - Run relevant tests after each significant change
   - Don't wait until the end to test
   - Fix failures immediately
   - Add new tests for new functionality

5. **Track Progress**
   - Keep TodoWrite updated as you complete tasks
   - Note any blockers or unexpected discoveries
   - Create new tasks if scope expands
   - Keep user informed of major milestones

### Phase 3: Quality Check

1. **Run Core Quality Checks**

   Always run before submitting:

   ```bash
   # Run full test suite (use project's test command)
   # Run linting (per CLAUDE.md)
   ```

2. **Consider Reviewer Agents** (Optional)

   Use for complex, risky, or large changes. Read agents from `.claude/compound-engineering.md` frontmatter (`review_agents`).

   Run configured agents in parallel with Task tool. Present findings and address critical issues.

3. **Final Validation**
   - All TodoWrite tasks marked completed
   - All tests pass
   - Linting passes
   - Code follows existing patterns
   - No console errors or warnings

### Phase 4: Ship It

1. **Create Commit**

   ```bash
   # Stage specific files related to this work (not git add .)
   git add <files related to this feature>
   git status  # Review what's being committed
   git diff --staged  # Check the changes

   # Commit with conventional format
   git commit -m "$(cat <<'EOF'
   feat(scope): description of what and why

   Brief explanation if needed.

   Co-Authored-By: Claude <noreply@anthropic.com>
   EOF
   )"
   ```

2. **Create Pull Request**

   Use the base branch from config (read in Phase 1 step 2) as the PR target:

   ```bash
   git push -u origin <person>/<short-description>

   gh pr create --base [base_branch] --title "<type>: [Description]" --body "$(cat <<'EOF'
   ## Summary
   - What changed
   - Why it was needed
   - Key decisions made

   Closes #N

   ## Testing
   - Tests added/modified
   - Manual testing performed
   EOF
   )"
   ```

   Include `Closes #N` (or `Fixes #N` for bugs) so issues auto-close on merge,
   per CLAUDE.md PR conventions.

3. **Blocked-Issue Cleanup**

   After PR creation, check if any issues with the `blocked` label depended
   on the work just completed. If all their dependencies are now resolved,
   remove the `blocked` label and move to "Ready" on the project board.

   ```bash
   gh issue list --label blocked --state open --json number,title,body
   ```

4. **Resource Cleanup**

   After PR creation, clean up ephemeral working docs:

   1. If a brainstorm file was used as input, check whether any other
      open branches reference it (`git log --all --oneline -- <file>`).
      If no other branches reference it, delete it.
   2. Same check for the plan file.
   3. Delete any `.claude/todos/agent/` working files for completed items.
   4. Commit the deletions as part of the PR.

4. **Update Plan Status**

   If the input document has YAML frontmatter with a `status` field, update it to `completed`:
   ```
   status: active  →  status: completed
   ```

5. **Notify User**
   - Summarize what was completed
   - Link to PR
   - Note any follow-up work needed
   - Suggest next steps if applicable

## Key Principles

### Start Fast, Execute Faster

- Get clarification once at the start, then execute
- Don't wait for perfect understanding - ask questions and move
- The goal is to **finish the feature**, not create perfect process

### The Plan is Your Guide

- Work documents should reference similar code and patterns
- Load those references and follow them
- Don't reinvent - match what exists

### Test As You Go

- Run tests after each change, not at the end
- Fix failures immediately
- Continuous testing prevents big surprises

### Quality is Built In

- Follow existing patterns
- Write tests for new code
- Run linting before pushing
- Use reviewer agents for complex/risky changes only

### Ship Complete Features

- Mark all tasks completed before moving on
- Don't leave features 80% done
- A finished feature that ships beats a perfect feature that doesn't

## Quality Checklist

Before creating PR, verify:

- [ ] All clarifying questions asked and answered
- [ ] All TodoWrite tasks marked completed
- [ ] Tests pass (run project's test command)
- [ ] Linting passes
- [ ] Code follows existing patterns
- [ ] Commit messages follow conventional format
- [ ] PR description includes summary and testing notes
[A1 05:19] I'll start by reading the plan document and the current state of the key files.
  -> Read: 2026-02-20-feat-mind-meld-cooperative-redesign-plan.md
  -> Read: types.ts
  -> Read: types.ts
  -> Read: play.ts
[A6 05:19] Now let me read the remaining files I need.
  -> Read: server.ts
  -> Read: config.ts
  -> Read: scoring.ts
  -> Read: meld.ts
  -> Read: vocab.ts
[A12 05:19] Let me also check the existing test files and api.ts.
  -> Read: api.ts
  -> Glob: src/server/play.test.ts
  -> Read: play.test.ts
[A16 05:19] I've read through the full plan and all relevant files. The scope is clear — Phase 3 rewires the server game loop: types, phase transitions, meld detection, scoring failure fallback, centroid-as-prompt cycling, game end, and vocab threading. I'll work through 3a–3i in order, committing after each sub-task.

Let me verify the branch and set up.
  -> Bash: git branch --show-current
  -> Bash: mkdir -p /Users/rhhart/Documents/GitHub/schelling-points/.claude/todos/agent
[A19 05:20] Good, on `hart/mind-meld`. Let me set up the task tracking and start working.
  -> TodoWrite
[A21 05:20] ## 3a: Wire Protocol Types (`src/types.ts`)

Replacing `SCORE_STATE`/`SCORES_READY` with `REVEAL_STATE`/`REVEAL_READY`, renaming `category` → `prompt` in `GUESS_STATE`, adding `GAME_END`, `CONTINUE_PROMPT`, `CONTINUE_VOTE`.
  -> Write: types.ts
[A23 05:20] Now let me commit 3a and move to 3b.
  -> Bash: git add src/types.ts && git commit -m "$(cat <<'EOF'
feat(types): update wire pr
  -> TodoWrite
[A26 05:20] ## 3b: Server Types (`src/server/types.ts`)

Updating Phase union (SCORES→REVEAL, add CONTINUE), Game class (remove categoryQueues, add centroidHistory/currentPrompt/scoringRetries), RoundScore (category→prompt), State (add vocab).
  -> Write: types.ts
  -> Bash: git add src/server/types.ts && git commit -m "$(cat <<'EOF'
feat(server/types): 
  -> TodoWrite
[A30 05:21] ## 3c–3i: Full play.ts rewrite

Sub-tasks 3c through 3i are tightly coupled — they all modify play.ts and each depends on the previous one. I'll implement them all together in a single coherent rewrite, then commit.
  -> Write: play.ts
[A32 05:22] The `timeSecs` diagnostic is just a hint (not an error) — that parameter was already unused before my changes, and it's part of the tick interface. I'll leave it as-is.

Now let me update `server.ts` to load vocab at startup (3h).
  -> Write: server.ts
[A34 05:22] Now let me check if there are any other files that reference the old types (`SCORE_STATE`, `SCORES_READY`, `category` in GUESS_STATE context, or construct `State` without vocab).
  -> Grep: SCORE_STATE|SCORES_READY|SCORES in src
  -> Grep: new t\.State\(|new State\( in src
[A37 05:22] The client-side references (`client.tsx`, `client/types.ts`, `Scores.tsx`) are Phase 5's job — leaving those alone. But the test file needs updating for the new types and API signatures. Let me also check if there's anything else I need to update.
  -> Read: play.test.ts
[A39 05:22] I need to update the tests for the new API: `category`→`prompt`, `SCORES`→`REVEAL`, add `centroidWord` to stub, pass `state` instead of `categories` to `onTickGame`, and add `vocab` to `State` constructor.
  -> Write: play.test.ts
[A41 05:23] Now let me try to compile to see if everything holds together.
  -> Bash: bunx tsc --noEmit 2>&1 | head -60
  -> Bash: npx tsc --noEmit 2>&1 | head -80
  -> Bash: cat tsconfig.json | head -5
  -> Glob: **/tsconfig*.json
[A46 05:23] No project-level tsconfig. Let me check what the build script uses.
  -> Read: package.json
  -> Glob: vite.config.*
  -> Read: vite.config.ts
[A50 05:23] No tsconfig — the project relies on Vite for building. Let me just run `bunx vitest run` to check the tests, and also do a quick Vite build to verify compilation.
  -> Bash: bunx vitest run 2>&1
  x Bash failed: Exit code 1

[1m[46m RUN [49m[22m [36mv4.0.18 [39m[90m/Users/rhhart/Documents/GitHub/schelling-points[39m

 [31m❯[39m src/server/play.test.ts [2m([22m[2m0 test[22m[2m)[22m
 [32m✓[39m
[A52 05:23] All 57 tests pass. The only failure is `play.test.ts` which can't even load because of the pre-existing `bun:test` import — exactly as the instructions said to expect. The test code itself is correct.

Let me also verify Vite can build the client (since the client still references old types, I expect some errors there, but that's Phase 5's job).
  -> Bash: bun run build 2>&1 | tail -30
[A54 05:23] Vite build succeeds too — the client code compiles even though it still references old message types (Vite/esbuild doesn't do strict TS type checking, but the code transforms fine).

Now let me commit all the Phase 3 changes.
  -> Bash: git add src/server/play.ts src/server.ts src/server/play.test.ts && git commit -
[A56 05:23] Now let me mark off the plan checkboxes and update todos.
  -> Read: 2026-02-20-feat-mind-meld-cooperative-redesign-plan.md
  -> Edit: 2026-02-20-feat-mind-meld-cooperative-redesign-plan.md
  -> Bash: git add docs/plans/2026-02-20-feat-mind-meld-cooperative-redesign-plan.md && git
  -> TodoWrite
[A61 05:24] ## Phase 3 Complete

Here's what was done across 4 commits on `hart/mind-meld`:

**3a — Wire protocol** ([src/types.ts](src/types.ts)):
- `SCORE_STATE` → `REVEAL_STATE` (with `centroidWord`, `centroidIsRepeat`, `melded`; no `playerScores`)
- `SCORES_READY` → `REVEAL_READY`
- `category` → `prompt` in `GUESS_STATE`
- Added `GAME_END`, `CONTINUE_PROMPT`, `CONTINUE_VOTE` message types

**3b — Server types** ([src/server/types.ts](src/server/types.ts)):
- Phase: `SCORES` → `REVEAL` (gains `centroidWord`, `melded`), added `CONTINUE` phase, `category` → `prompt`
- Game: removed `categoryQueues`, added `centroidHistory`, `currentPrompt`, `scoringRetries`
- `RoundScore.category` → `.prompt`
- `State`: added `vocab` field, updated constructor

**3c–3i — play.ts rewrite** ([src/server/play.ts](src/server/play.ts)):
- `scoreRound`: filters prompt repetitions, detects meld, threads `state.vocab` to `scoreGuesses`
- `goToNextRound`: centroid word becomes next prompt; meld → `endGame(melded=true)`; round 20 → `endGame(melded=false)` (Phase 4 changes this to CONTINUE)
- `endGame()` and `buildPlayerHistory()` extracted as named, exported functions
- `handleScoringFailure`: retries same prompt up to `MAX_SCORING_RETRIES`, then ends game
- `currentGameState`: `REVEAL_STATE` with `centroidIsRepeat` computed fresh from history; `CONTINUE` case returns safe fallback
- `state` threaded through entire call chain (`onTickGame` → `scoreRound` → `goToNextRound` → `endGame`)
- `JOIN_LOUNGE` handler now unicasts `LOUNGE` message back to joining player
- `REVEAL_READY` handler replaces `SCORES_READY`
- `CONTINUE_VOTE` handler stubbed (Phase 4)

**3h — server.ts** ([src/server.ts](src/server.ts)): loads vocab at startup, passes to `State` constructor

**Tests**: 57/57 pass. Only failure is the pre-existing `bun:test` import in `play.test.ts` (test code itself updated for new API).

**Build**: `bun run build` succeeds.

Ready for further instructions — Phase 4 (CONTINUE phase) is next in the plan.
[U2 05:25] awesome work. is phase 3 totally done? does phase 4 have everything it needs to complete?
[A62 05:25] Good questions. Let me verify both claims systematically.
  -> Task
[A64 05:26] **Phase 3 is fully complete.** Every item checks out:

- All 4 functions Phase 4 needs are exported: `endGame()`, `buildPlayerHistory()`, `newGuessPhase(round, prompt)`, `currentGameState()`
- Wire protocol types all defined: `CONTINUE_VOTE`, `CONTINUE_PROMPT`, `GAME_END`
- `CONTINUE` phase in the Phase union, `onTickGame`, and `currentGameState`
- `CONTINUE_VOTE` stub in `onClientMessage` (ready for Phase 4 to fill in)
- `goToNextRound` calls `endGame()` at round 20 (Phase 4's hook point)

**Phase 4 has everything it needs to start.** The three things it must create:

1. **`checkContinueVotes()` function** in `play.ts` — new helper, must be exported for `api.ts`
2. **`CONTINUE_VOTE` handler body** — fill in the stub (vote tracking, "I'm out" mechanic)
3. **Disconnect handler update** in `api.ts` — add game-player disconnect handling during CONTINUE phase

No blockers. Ready when you are.
[U3 05:26] run the /handoff-test and create a great handoff prompt for the next working agent for phase 4
  -> Skill: 
[U4 05:26] Base directory for this skill: /Users/rhhart/.claude/skills/handoff-test

# Handoff Test

## The Naive Reader

A fresh Claude instance with no session history. Technically competent — can read the codebase, run code, check git history. Has access to standard tooling, CLAUDE.md, and memory files. But has seen nothing from this conversation: no decisions made, options rejected, constraints discovered, or terminology coined. The only things that exist for the naive reader are what's written in persistent artifacts.

The test: could that instance pick up the artifact and execute without asking clarifying questions?

## Process

1. Identify what was created or modified this session. Read each artifact in full.
2. From the naive reader perspective, find implicit dependencies:
   - Session-local terminology used without definition
   - References to "the bug," "the approach," "what we discussed" without grounding
   - Decisions recorded without rationale (a future reader needs *why* to know when the decision expires)
   - Scope assumed but not stated
   - Next steps that depend on conversation memory
3. Report gaps. For each: what's missing, where it goes, concrete suggested text. Offer to apply fixes.

If the artifact passes, say so in one sentence.
[A66 05:27] I'll audit the artifacts from this session for handoff gaps. Let me read each one a naive reader would encounter.
  -> Read: play.ts
  -> Read: 2026-02-20-feat-mind-meld-cooperative-redesign-plan.md
[A69 05:27] Now let me check the specific code spots Phase 4 will touch and compare against the plan's assumptions.
  -> Read: play.ts
  -> Read: play.ts
  -> Read: play.ts
  -> Read: api.ts
[A74 05:27] Good. I've now audited everything. Here's the handoff test result:

---

## Handoff Test Report

### Verdict: The plan document is **mostly handoff-safe**, with 3 gaps a naive reader would hit.

### Gap 1: Plan sub-task 3 is already done

**What's missing:** The plan's Phase 4 sub-task 3 says "Add a no-op case in `onTickGame`" for CONTINUE — but Phase 3 already added this at [play.ts:84-87](src/server/play.ts#L84-L87). A naive reader would try to add it, find it already exists, and wonder what else was already done.

**Where it goes:** Handoff prompt context.

### Gap 2: Plan's CONTINUE_VOTE snippet has a unicast-after-removal bug

**What's missing:** The plan's sub-task 2 code shows:
1. `game.players = game.players.filter(...)` (removes player)
2. `game.unicast(message.playerId, loungeMsg)` (tries to send via player lookup)

But `game.unicast` searches `game.players` — the player was just removed. The LOUNGE message would silently never arrive. The agent needs to send via `player.webSocket.send(JSON.stringify(loungeMsg))` directly, using the `player` reference captured before the filter.

**Where it goes:** Handoff prompt constraint.

### Gap 3: Plan snippet vs actual code structure in `goToNextRound`

**What's missing:** The plan's sub-task 1 shows `if (round + 1 >= MAX_ROUNDS && !phase.melded)` as a single condition. But in the actual code, `round` is already `phase.round + 1` (line 433), and the melded check is a separate early return (lines 435-438). The naive reader needs to know: **replace the block at lines 441-445**, don't add a new condition.

**Where it goes:** Handoff prompt context.

---

Now let me write the handoff prompt that addresses all three gaps:
[A75 05:28] Good. I've now audited all artifacts against what a naive reader would encounter. Here's my report:

---

## Handoff Test Report

### Artifacts audited
- [src/server/play.ts](src/server/play.ts) (primary — rewritten this session)
- [src/server/types.ts](src/server/types.ts), [src/types.ts](src/types.ts), [src/server.ts](src/server.ts) (modified)
- [src/server/api.ts](src/server/api.ts) (Phase 4 will modify)
- [docs/plans/2026-02-20-feat-mind-meld-cooperative-redesign-plan.md](docs/plans/2026-02-20-feat-mind-meld-cooperative-redesign-plan.md) (the plan Phase 4 follows)

### Gaps Found: 3

**Gap 1: Plan sub-task 3 is already done — naive reader would duplicate work**

The plan says Phase 4 sub-task 3 is "Add a no-op case in `onTickGame`" for CONTINUE. But Phase 3 already added this ([play.ts:84–87](src/server/play.ts#L84-L87)). A naive reader following the plan literally would try to add it and either get confused or create a duplicate.

> *Suggested fix:* Note in handoff prompt that sub-task 3 is pre-completed.

**Gap 2: Plan's CONTINUE_VOTE snippet has a sequencing bug**

The plan's sub-task 2 code does this in order:
1. `game.players = game.players.filter(...)` — removes the leaving player
2. `game.unicast(message.playerId, loungeMsg)` — tries to unicast to them

But `game.unicast` looks up the player in `game.players` ([play.ts:41–52 in types.ts](src/server/types.ts)). After step 1 removes them, the unicast silently fails. The comment "still has their socket" is misleading.

> *Fix:* The departing player's `webSocket` is captured in the `player` variable before filtering. Use `player.webSocket.send(JSON.stringify(loungeMsg))` directly instead of `game.unicast(message.playerId, loungeMsg)`.

**Gap 3: Plan's round-20 snippet uses different variable convention than actual code**

The plan's sub-task 1 shows `if (round + 1 >= MAX_ROUNDS && !phase.melded)`. But the actual `goToNextRound` ([play.ts:433](src/server/play.ts#L433)) already computes `const round = phase.rou [...truncated]
  -> Write: phase-4-handoff.md
[A77 05:29] Here's the summary:

## Handoff Test Results

**3 gaps found** in the plan document that would trip up a naive reader:

1. **Sub-task 3 is already done** — the CONTINUE no-op in `onTickGame` was built in Phase 3. A naive reader would try to add it again.

2. **Unicast-after-removal bug** in the plan's CONTINUE_VOTE snippet — the plan removes the player from `game.players` then calls `game.unicast()` which looks up the player in `game.players`. Silent failure. Fix: use `player.webSocket.send()` directly.

3. **Variable convention mismatch** — the plan's snippet uses `round + 1 >= MAX_ROUNDS` but the actual code already computes `round = phase.round + 1`. The agent needs to know they're *replacing* the existing block at lines 441–445, not adding a new condition.

## Handoff Prompt

Written to [.claude/todos/agent/phase-4-handoff.md](.claude/todos/agent/phase-4-handoff.md). It:

- Lists exactly what Phase 3 built and where to find it (no session-memory needed)
- Calls out all 3 gaps with corrections
- Specifies exact line numbers and replacement targets
- Flags the plan's unicast bug with the fix
- Notes which sub-task to skip
- Includes testing and constraint reminders

To use it, start the next agent with:
```
/workflows:work on Phase 4 using .claude/todos/agent/phase-4-handoff.md as the primary input
```
[U5 05:30] can you add the plan's relative path to the prompt and re-send?
[A78 05:30] Good catch — the handoff prompt references the plan but doesn't give the path explicitly for the agent to find it.
  -> Read: phase-4-handoff.md
  -> Edit: phase-4-handoff.md
[A81 05:31] Done. The handoff now explicitly points to `docs/plans/2026-02-20-feat-mind-meld-cooperative-redesign-plan.md` and tells the agent where to find Phase 4 within it (line ~795), plus makes clear this handoff doc takes precedence where they conflict.

To kick it off:
```
/workflows:work on Phase 4 using .claude/todos/agent/phase-4-handoff.md as the primary input
```
